# Local wrangler pages dev ONLY — placeholder values, never real secrets.
# Real secrets are set in the Cloudflare dashboard at go-live (GO-LIVE.md).
SESSION_SECRET=dev-session-secret-not-for-production
ADMIN_TOKEN=dev-admin-token
DEV_MODE=1
